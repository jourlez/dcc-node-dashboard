name: Built Release

on:
  push:
    branches:
      - develop
      - main
      - production

jobs:
  built-release:
    uses: decentralchain/.github/.github/workflows/built-release.yml@main
    with:
      node: 20
